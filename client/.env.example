# Only needed when the frontend and backend are deployed as separate services
# (e.g. frontend on Vercel, backend on Render). Leave unset for local dev or a
# single-service deploy, where the frontend and API share an origin.
VITE_API_BASE=https://your-backend.onrender.com
